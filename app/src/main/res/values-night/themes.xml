<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WATmerch" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/background_second</item>
        <item name="colorPrimaryVariant">@color/foreground_first</item>
        <item name="colorOnPrimary">@color/foreground_second</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/background_second</item>
        <item name="colorSecondaryVariant">@color/foreground_second</item>
        <item name="colorOnSecondary">@color/foreground_first</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/viga</item>
        <item name="bottomNavigationStyle">@style/BottomNavigationStyle</item>
    </style>
</resources>